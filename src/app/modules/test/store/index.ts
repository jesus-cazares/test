import { Action, ActionReducerMap } from '@ngrx/store';

export const featureKey = 'registration';

export interface State {
}

export const reducer: ActionReducerMap<State, Action> = {
};

export const effects = [
];
